<% include ../partials/header %>
<div class="ui main text container " style="width:40%">
    <div class="ui huge header">
         <%= user.firstName + " " + user.lastName %>
    </div>
    <div class="ui segment pp"><div id="pp1">
    	<div class="ui top left attached label">Email me at: <a><%= user.email %></a></div>
    <img class="ui medium rounded left floated image" src="<%= user.avatar %>" alt="user profile image">
		<strong>About</strong><hr>
		<p><%=user.about%></p>
		</div>
		<div id="usblogs">
		<h3><%= user.username %>'s blogs:</h3>
		<ol>
			
		<% blogs.forEach(function(blog) { %>
			<li><a href="/blogs/<%= blog.id %>"><%= blog.title %></a></li>
		<% }); %>
		</ol>
		
	</div>
     	</div>  
</div>

<% include ../partials/footer %>